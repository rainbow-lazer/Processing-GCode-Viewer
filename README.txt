Code by Chris Reilly of DIYLILCNC.org

This is an incredibly bare-bones GCode renderer. I made this in just a couple hours, so it only parses X/Y/Z positions (no arcs or anything fancy like that). This script could most likely be extended by looking at the reprap's arduino geode interpreter:

http://reprap.org/wiki/Arduino_GCode_Interpreter 